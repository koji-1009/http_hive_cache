import 'package:http_hive_cache/src/types.dart';

Future<HttpHiveResponse> performGet(Uri url, {Map<String, String>? headers}) {
  throw UnsupportedError('HTTP GET is not supported on this platform.');
}
